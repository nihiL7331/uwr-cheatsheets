# Metody Programowania

## Kompilacja

```bash
# kompilacja z poziomu root tego repozytorium
typst compile --input od=[od] --input do=[do] --font-path fonts/ obowiazki/mp_metody-programowania/cheatsheet.typ [output_file]
# od: 1-15 (default: 1)
# do: 1-15 (default: 15)
```
