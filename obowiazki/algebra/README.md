# Algebra

## Kompilacja

```bash
# kompilacja z poziomu root tego repozytorium
typst compile --input od=[od] --input do=[do] --input scheme=[scheme] --font-path fonts/ obowiazki/algebra/cheatsheet.typ [output_file]
# od: 1-15 (default: 1)
# do: 1-15 (default: 15)
# scheme: light/dark (default: light)
```
